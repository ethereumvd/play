#include<bits/stdc++.h>
using namespace std;
int main()
{
    /* this is just for understanding find() and erase() 
    
    this code removes all occurences of part substring from s

    string s ; getline(cin,s);
    string part ; getline(cin,part);
    cout << s.find(part);
    cout << "\n";
    cout << s.erase(s.find(part),part.length());
    */
    

}

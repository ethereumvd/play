#include<bits/stdc++.h>
using namespace std;
void jogo() {
    int n;
    cin>>n;
    vector<int> v(n+1),a(n+1);
    for(int i=1;i<=n;i++) cin>>v[i];
    for(int i=1; i<=n;i+=) a[i] = i;
    reverse(v.begin(),v.end());
    for(int i=1;i<=n;i++) {
        cout << " Answer " << " Nigga " << endl;
    }
}
int32_t main() {
    ios::sync_with_stdio(false) ,cin.tie(nullptr) ,cout.tie(nullptr) ;
    int tc=1 ;
    while(tc--) jogo() ;
}

#include<bits/stdc++.h>
using namespace std;
void jogo() {
    int n , x , m ; cin >> n >> x >> m ;
    while(m--) {
        int l , r ; cin >> l >> r;
    }
}
int32_t main() {
    ios::sync_with_stdio(false) ,cin.tie(nullptr) ,cout.tie(nullptr) ;
    int tc ; cin >> tc ;
    while(tc--) jogo() ;
    return 0;
}

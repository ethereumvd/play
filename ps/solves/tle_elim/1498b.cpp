#include<bits/stdc++.h>
using namespace std;
void jogo() {

    int n , w ; cin >> n >> w ;
    vector<int> wt(n) ;
    for(int &i:wt) cin >> i ;


}
int32_t main() {
    ios::sync_with_stdio(false) ,cin.tie(nullptr) ,cout.tie(nullptr) ;
    int tc = 1 ; cin >> tc ;
    while(tc--) jogo() ;
    return 0;
}
